# Dependencias de los scripts de extracción/generación SERNATUR (Python 3.11+)
# Instalar en Windows con:
#     pip install -r requirements.txt
#     playwright install chromium
playwright>=1.44
deep-translator>=1.11
openpyxl>=3.1
