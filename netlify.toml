# Despliegue continuo Netlify — PWA Turismo Cochrane
[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA: todas las rutas sirven index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
